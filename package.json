{
  "name": "appetit-mvp",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "pnpm -C apps/api dev",
    "build": "pnpm -C apps/api build"
  },
  "prisma": {
    "seed": "ts-node prisma/seed.ts"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "concurrently": "^9.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
